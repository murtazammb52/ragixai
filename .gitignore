# Environment / secrets
.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
.venv/
*.egg-info/
dist/
build/
*.egg

# Data — raw filings not committed (too large, re-download with /ingest-docs)
data/raw/

# Chunks committed to git (17 MB) — teammates skip download+chunking
# ChromaDB committed via Git LFS (170 MB) — teammates skip the 60-min embedding build
# data/processed/ and data/chroma_db/ are intentionally NOT ignored here

# Logs
*.log
pip_install.log
build_index.log

# Node
node_modules/
package-lock.json

# OS / editor
.DS_Store
Thumbs.db
~$*
*.tmp

# Claude Code session data (keep commands/hooks/settings — those are shared config)
.claude/projects/
.claude/*.jsonl
.claude/*.json.bak

# Jupyter
.ipynb_checkpoints/
